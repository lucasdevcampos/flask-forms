# 📝 Simple Flask-WTF Form

This project demonstrates how to create a **basic form** using [Flask](https://flask.palletsprojects.com/) and [Flask-WTF](https://flask-wtf.readthedocs.io/).  
It’s a minimal example to show how to handle form input, validation, and rendering with templates.

---

## 🚀 Features
- Simple form with one or more input fields
- CSRF protection enabled via `SECRET_KEY`
- Validation using WTForms validators
- Clean HTML template with Jinja2 integration

---

## 📂 Project Structure
- form.py (main application)
- templates
- form.html
